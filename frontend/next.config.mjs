// frontend/next.config.mjs
export default {
    reactStrictMode: true,
    // Optional if you are still using other settings
    experimental: {
        appDir: true // Enable App Directory (in case you're using it)
    }
};
